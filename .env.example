# Пример переменных окружения. РЕАЛЬНЫЕ значения — только в .env (в git не идёт) или в Keychain.
# Достать секрет: security find-generic-password -a claude-workspace -s <service> -w

PORT=3000
NODE_ENV=development

# DATABASE_URL=
# API_TOKEN=
